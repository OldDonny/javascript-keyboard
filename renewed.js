var sentenceCounter=0


function getSentence(){
    var currentSentence=sentence[sentenceCounter]
    sentenceCounter++;
    var splitSentence=currentSentence.split('');
    splitSentence.array.forEach(function(character,index) {
        $('#sentence-container').apend 
    });
}
